---
import Section from "../components/Section.astro";
import { skills } from "@/cv.json";
---


<Section title="Habilidades" >

    <div class="flex flex-col gap-2 w-full" id="skills">
        {skills.map( (skill)=> (
            <div class="mx-auto flex flex-row gap-3 bg- p-1 font-medium">
                <h2>{skill.name} - {skill.description}</h2>
                              
            </div>
           

        ) )}
    </div>

</Section>